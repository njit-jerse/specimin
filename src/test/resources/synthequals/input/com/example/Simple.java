package com.example;

import org.example.Type;

class Simple {
    // Target method.
    void bar() {
        Type[] types = null;
        if (types[0] == Type.INT) {
            // do something
        }
    }
}
